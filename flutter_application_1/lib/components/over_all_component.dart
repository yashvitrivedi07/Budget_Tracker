import 'package:flutter/material.dart';

class OverAllComponent extends StatelessWidget {
  const OverAllComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [Text("Over All")],
    );
  }
}
